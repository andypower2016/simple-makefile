
strA = HelloA
strB = HelloB

strC := $(subst ll,LL,$(strB))  # check if $(strB) has ll, if does, replace it with LL

all:
ifeq ("aaa","bbb")
	$(error "equal")
else
	$(warning "warning test")
	echo $(strC)
endif

#find string "LLo" in $(strC), if found, reture LLo, if not, return empty 
	findStr=$(findstring LLo, $(strC)) 
	echo $(findStr)



